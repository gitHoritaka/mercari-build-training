name: Docker Build and Push

on:
  push:
    branches:
      - main
      - 'feature/*'

jobs:
  build_and_push:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set Docker Tag
        id: set_tag
        run: echo "DOCKER_TAG=ghcr.io/githoritaka/mercari-build-training:${GITHUB_REF##*/}" >> $GITHUB_ENV

      - name: Build Docker Image
        run: docker build -t $DOCKER_TAG -f python/dockerfile .

      - name: Push Docker Image to GitHub Container Registry
        run: docker push $DOCKER_TAG
      
      - name: Pull Docker Image
        run: docker pull $DOCKER_TAG

      - name: Run Docker Image 
        run: docker run -d -p 9000:9000 $DOCKER_TAG 
      
      - name: start server
        run: uvicorn python/main:app --reload --port 9000

      - name: Run test.py
        run: python3 python/test.py
